# App
NODE_ENV=development

# Python interpreter path (leave empty to auto-detect)
PYTHON_PATH=

# R interpreter path (leave empty to auto-detect)
R_PATH=

# Analysis timeout in seconds
ANALYSIS_TIMEOUT=300
